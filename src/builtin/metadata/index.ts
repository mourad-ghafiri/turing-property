// ============================================================================
// METADATA - Re-exports
// ============================================================================

// Common metadata (label, hint, description, visibility, etc.)
export * from './common';

// Input-specific metadata (inputType, autocomplete, step, min, max, etc.)
export * from './input';

// Format metadata (format, pattern, unit, currency, etc.)
export * from './format';

// Layout metadata (layout, gap, width, height, variant, etc.)
export * from './layout';

// Collection metadata (options, maxItems, searchable, sortable, etc.)
export * from './collection';

// Container metadata (isGroup, isForm, isWizard, collapsible, etc.)
export * from './container';

// Action metadata (submitLabel, nextLabel, showProgress, etc.)
export * from './action';
